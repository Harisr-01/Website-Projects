# Create a folder locally
mkdir ASP.NETDataSessionTable
# Navigate into the folder
cd ASP.NETDataSessionTable

# Create a dummy file (required for Git to recognize the folder)
touch Desc.txt

# Add and commit the changes
git add .
git commit -m "Added folder and file"

# Push changes to GitHub
git push origin main
